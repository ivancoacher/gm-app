<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jsnjwj.compare.dao.CContractRecordDao">

    <select id="selectGroupData" resultType="com.jsnjwj.compare.response.CompareAnalysisChartResponse">
        select count(*) as count ,operate_day as label from c_contract_record
        where create_time <![CDATA[ >= ]]> #{startTime}
          and create_time <![CDATA[ <= ]]> #{endTime}
        GROUP BY operate_day
    </select>
</mapper>